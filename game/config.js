export default {
  backgroundImageStartingSize: 1200,
  virusStartingSize: 32,
  frameRate: 60,
  acceleration: 7.5, // velocity change per second when player is moving
  maxSpeed: 180, // displacement per second (per dimension)
};
